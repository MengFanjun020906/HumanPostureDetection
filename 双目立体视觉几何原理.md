# 双目立体视觉几何原理与代码对应分析

## 1. 相机模型与内参矩阵

### 1.1 针孔相机模型

针孔相机模型是计算机视觉的基础，它将三维世界点映射到二维图像平面。

#### 几何关系

针孔相机模型是一个简化的相机成像模型，其几何关系如下：
1. **光心** (Camera Center, O)：相机的光学中心，所有光线汇聚于此点
2. **成像平面** (Image Plane)：捕获图像的平面，与光轴垂直
3. **光轴** (Optical Axis)：通过光心并垂直于成像平面的直线
4. **焦距** (Focal Length, f)：光心到成像平面的距离
5. **主点** (Principal Point, (c_x, c_y))：光轴与成像平面的交点

在针孔模型中，三维空间点 (X, Y, Z) 通过光心投影到成像平面上的二维点 (u, v)，满足相似三角形关系。

#### 投影公式

三维世界点 $\mathbf{X} = [X, Y, Z]^T$ 到图像点 $\mathbf{x} = [u, v]^T$ 的投影关系为：

$$
\begin{bmatrix} u \\ v \\ 1 \end{bmatrix} = \frac{1}{Z} \begin{bmatrix} f_x & s & c_x \\ 0 & f_y & c_y \\ 0 & 0 & 1 \end{bmatrix} \begin{bmatrix} X \\ Y \\ Z \end{bmatrix}
$$

其中：
- $f_x, f_y$：相机在x和y方向的焦距（像素单位）
- $c_x, c_y$：主点坐标（图像中心，像素单位）
- $s$：倾斜因子（通常为0）

#### 代码对应

在 `camera_calibrator_double.py` 中，相机内参矩阵 `K`（或 `camera_matrix`）表示为：

```python
# 示例内参矩阵
camera_matrix_left = np.array([
    [5628.451582711846, 0.0, 2698.685087632601],
    [0.0, 5675.814867786561, 1983.7792435461297],
    [0.0, 0.0, 1.0]
])
```

对应公式中的内参矩阵：

$$
K = \begin{bmatrix} f_x & 0 & c_x \\ 0 & f_y & c_y \\ 0 & 0 & 1 \end{bmatrix}
$$

### 1.2 畸变校正

真实相机存在光学畸变，主要包括径向畸变和切向畸变。

#### 径向畸变

径向畸变由镜头形状引起，公式为：

$$
\begin{aligned}
x_{distorted} &= x(1 + k_1 r^2 + k_2 r^4 + k_3 r^6) \\ 
y_{distorted} &= y(1 + k_1 r^2 + k_2 r^4 + k_3 r^6)
\end{aligned}
$$

其中 $r^2 = x^2 + y^2$ 是归一化坐标到主点的距离平方，$k_1, k_2, k_3$ 是径向畸变系数。

#### 切向畸变

切向畸变由镜头与成像平面不平行引起，公式为：

$$
\begin{aligned}
x_{distorted} &= x + [2p_1 xy + p_2(r^2 + 2x^2)] \\ 
y_{distorted} &= y + [p_1(r^2 + 2y^2) + 2p_2 xy]
\end{aligned}
$$

其中 $p_1, p_2$ 是切向畸变系数。

#### 代码对应

在代码中，畸变系数存储为一维数组：

```python
# 示例畸变系数 (k1, k2, p1, p2, k3)
distortion_coeffs_left = np.array([-0.10538791331942947, 3.081316174389013, -0.0047049135841466545, 0.010629607799912915, -8.922728894760338])
```

去畸变处理使用OpenCV的 `undistortPoints` 函数：

```python
# 归一化坐标去畸变
imgpoints_left_norm = cv2.undistortPoints(imgpoints_left[0], mtx_left, dist_left, P=P1)
imgpoints_right_norm = cv2.undistortPoints(imgpoints_right[0], mtx_right, dist_right, P=P2)
```

## 2. 双目标定与外参

### 2.1 相对外参

双目系统中，右相机相对于左相机的位置和姿态由旋转矩阵 $R$ 和平移向量 $T$ 描述。

#### 旋转矩阵 $R$（3×3）

旋转矩阵是正交矩阵，满足 $R^T R = I$ 和 $\det(R) = 1$，表示右相机相对于左相机的旋转。

```python
# 示例旋转矩阵
rotation_matrix = np.array([
    [0.6777781499444254, -0.7299124568263363, -0.0885696608757976],
    [0.6638004337597015, 0.6592482940497991, -0.35321476601759133],
    [0.3162052554881784, 0.18060867133715203, 0.9313402945430228]
])
```

#### 平移向量 $T$（3×1）

平移向量表示右相机光心相对于左相机光心的位置，其长度为基线长度 $b$：

$$b = \|T\|
$$

```python
# 示例平移向量
translation_vector = np.array([1.4166373585569374, 9.173870151289641, -6.608207161709112])
# 计算基线长度
baseline_length = np.linalg.norm(translation_vector)
```

### 2.2 投影矩阵

相机投影矩阵 $P$（3×4）结合了内参和外参，将三维点投影到二维图像平面。

#### 左相机投影矩阵

左相机通常作为参考，外参为单位矩阵和零向量：

$$P_{left} = K_{left} [I | 0]
$$

```python
# 左相机投影矩阵
P1 = np.hstack((mtx_left, np.zeros((3, 1))))
```

#### 右相机投影矩阵

右相机投影矩阵包含相对外参：

$$P_{right} = K_{right} [R | T]
$$

```python
# 右相机投影矩阵
P2 = np.hstack((mtx_right @ R, mtx_right @ T))
```

## 3. 立体视觉的几何关系

### 3.1 对极约束

对极约束是双目视觉的基本几何约束，描述了左右图像中对应点之间的关系。

#### 本质矩阵（Essential Matrix）

本质矩阵 $E$ 描述了归一化相机坐标之间的关系：

$$E = [T]_{\times} R
$$

其中 $[T]_{\times}$ 是平移向量的反对称矩阵：

$$[T]_{\times} = \begin{bmatrix} 0 & -T_z & T_y \\ T_z & 0 & -T_x \\ -T_y & T_x & 0 \end{bmatrix}
$$

#### 基础矩阵（Fundamental Matrix）

基础矩阵 $F$ 描述了像素坐标之间的关系，与本质矩阵的关系为：

$$F = K_{right}^{-T} E K_{left}^{-1}
$$

对极约束可以表示为：

$$x_{right}^T F x_{left} = 0
$$

其中 $x_{left}, x_{right}$ 是左右图像中对应点的齐次坐标。

### 3.2 三角测量

三角测量是通过左右图像中的对应点计算三维坐标的过程。

#### 线性三角测量

使用投影矩阵 $P_{left}$ 和 $P_{right}$，对于对应点 $x_{left}$ 和 $x_{right}$，求解三维点 $X$：

$$
\begin{aligned}
X^T P_{left}^T x_{left}^{\times} P_{left} X &= 0 \\ 
X^T P_{right}^T x_{right}^{\times} P_{right} X &= 0
\end{aligned}
$$

这可以通过求解线性方程组得到齐次解 $X_{4d}$，然后转换为3D坐标：

$$X_{3d} = X_{4d} / X_{4d}[3]
$$

#### 代码对应

在 `camera_calibrator_double.py` 中，三角测量使用OpenCV的 `triangulatePoints` 函数：

```python
# 三角测量重建三维点
points_4d = cv2.triangulatePoints(P1, P2, imgpoints_left_norm, imgpoints_right_norm)
points_3d = points_4d[:3] / points_4d[3]  # 齐次坐标转换为3D坐标
```

### 3.3 视差与深度

视差 $d$ 是左右图像中对应点的水平像素差异，与深度 $Z$ 成反比：

$$Z = \frac{fb}{d}
$$

其中：
- $f$：焦距（像素单位）
- $b$：基线长度（米）
- $d = x_{left} - x_{right}$：视差（像素）

这是立体视觉深度计算的基本公式，表明视差越大，物体距离相机越近。

## 4. 立体校正

立体校正的目的是将左右图像的极线对齐到同一水平线上，简化对应点搜索。

### 4.1 旋转矩阵计算

校正旋转矩阵 $R_{rect}$ 使极线水平，计算方法为：

$$
\begin{aligned}
R_{rect,left} &= R_{rect}
\ R_{rect,right} &= R_{rect} R
\end{aligned}
$$

其中 $R_{rect}$ 是使校正后右相机平移向量只有x分量的旋转矩阵。

### 4.2 校正投影矩阵

校正后的投影矩阵为：

$$
\begin{aligned}
P_{rect,left} &= K_{rect} [R_{rect,left} | 0]
\ P_{rect,right} &= K_{rect} [R_{rect,right} | T_{rect}]
\end{aligned}
$$

其中 $K_{rect}$ 是校正后的内参矩阵，$T_{rect}$ 是校正后的平移向量（只有x分量）。

### 4.3 代码对应

在 `camera_calibrator_double.py` 中，立体校正使用OpenCV的 `stereoRectify` 函数：

```python
# 立体校正
R1, R2, P1, P2, Q, roi_left, roi_right = cv2.stereoRectify(
    mtx_left, dist_left, mtx_right, dist_right, image_size,
    R, T, alpha=alpha, newImageSize=image_size
)
```

其中：
- `R1, R2`：左右相机的校正旋转矩阵
- `P1, P2`：校正后的投影矩阵
- `Q`：深度映射矩阵（用于从视差图计算深度）
- `roi_left, roi_right`：校正后有效的图像区域

## 5. 三维点重建与重投影误差

### 5.1 三维点重建

通过三角测量得到三维点后，可以将其重投影到左右图像，验证重建精度。

#### 重投影公式

三维点 $X$ 到左图像的重投影：

$$x_{reproj,left} = P_{left} X
$$

重投影到右图像：

$$x_{reproj,right} = P_{right} X
$$

#### 代码对应

```python
# 将重建的三维点重投影到左右图像
reprojected_left, _ = cv2.projectPoints(points_3d.T, np.zeros(3), np.zeros(3), mtx_left, dist_left)
reprojected_right, _ = cv2.projectPoints(points_3d.T, np.zeros(3), np.zeros(3), mtx_right, dist_right)
```

### 5.2 重投影误差

重投影误差用于评估标定精度，定义为检测点与重投影点之间的距离：

$$e = \sqrt{(u_{detected} - u_{reproj})^2 + (v_{detected} - v_{reproj})^2}
$$

#### 代码对应

```python
# 计算重投影误差
error_left_x = detected_left[0] - reproj_left[0]
error_left_y = detected_left[1] - reproj_left[1]
error_left = np.sqrt(error_left_x**2 + error_left_y**2)

error_right_x = detected_right[0] - reproj_right[0]
error_right_y = detected_right[1] - reproj_right[1]
error_right = np.sqrt(error_right_x**2 + error_right_y**2)
```

## 6. 系统误差分析

### 6.1 误差来源

1. **标定误差**：相机内参和外参的标定误差
2. **特征提取误差**：角点检测的精度误差
3. **图像噪声**：由相机传感器和光照条件引起
4. **畸变校正误差**：畸变模型的近似误差
5. **立体匹配误差**：对应点搜索的误匹配

### 6.2 误差优化

代码中实现了多种误差优化方法：

1. **多组图像标定**：使用多组棋盘格图像提高标定精度
2. **异常值剔除**：过滤重投影误差过大的点
3. **立体三维误差计算**：结合三维空间误差和重投影误差
4. **参数固定**：当单目标定质量高时，固定内参进行双目标定

```python
# 优化的立体三维误差计算
if len(objpoints) > 0 and j < len(objpoints[0]):
    actual_3d_point = objpoints[0][j].flatten()  # 实际物体点
    try:
        # 三角化得到重建点
        points_4d = cv2.triangulatePoints(P1, P2, left_norm, right_norm)
        points_3d = points_4d[:3] / points_4d[3]  # 齐次坐标转换为3D坐标
        
        # 计算三维空间欧氏距离误差
        reconstructed_3d_point = points_3d.flatten()
        if np.all(np.isfinite(reconstructed_3d_point)):
            error_3d_spatial = np.linalg.norm(actual_3d_point - reconstructed_3d_point)
            stereo_error = (error_left + error_right + error_3d_spatial) / 3  # 综合误差
    except Exception as e:
        pass  # 三角化失败时保持原误差
```

## 7. 代码结构与工作流程

### 7.1 主要功能模块

1. **图像配对**：自动匹配左右相机的图像对
2. **角点检测**：检测棋盘格角点
3. **单目标定**：分别标定左右相机的内参和畸变系数
4. **双目标定**：计算相机间的相对外参
5. **立体校正**：对齐左右图像的极线
6. **三维重建**：三角测量重建三维点
7. **误差评估**：计算重投影误差和三维空间误差

### 7.2 工作流程

1. **输入参数**：读取图像目录、棋盘格参数、输出目录等
2. **图像预处理**：读取并配对左右图像
3. **角点检测与验证**：检测棋盘格角点并验证一致性
4. **相机标定**：执行单目标定和双目标定
5. **立体校正**：计算校正参数
6. **三维重建验证**：重建棋盘格三维点并计算误差
7. **结果保存**：保存标定参数和验证结果

## 8. 应用与扩展

### 8.1 姿态绕杆检测应用

在篮球姿态绕杆检测应用中，双目立体视觉用于：

1. **三维坐标重建**：精确计算篮球和球员的三维位置
2. **运动轨迹分析**：跟踪球员和篮球的运动轨迹
3. **姿态估计**：基于三维点云估计球员的姿态
4. **绕杆判断**：根据三维位置判断是否完成绕杆动作

### 8.2 精度优化建议

1. **提高标定精度**：
   - 使用更大的棋盘格
   - 增加标定图像数量（15-20组）
   - 确保图像覆盖整个视场
   - 保持棋盘格平面与相机光轴垂直

2. **改进图像质量**：
   - 减少图像噪声（使用滤镜）
   - 确保光照均匀
   - 提高图像分辨率

3. **优化立体匹配**：
   - 使用更精确的特征提取算法
   - 增加特征点数量
   - 应用鲁棒的匹配算法

4. **系统校准**：
   - 定期重新标定相机
   - 考虑温度对相机参数的影响
   - 确保相机安装稳定

## 9. 总结

双目立体视觉是计算机视觉中实现三维感知的重要技术，通过两个相机的视差信息可以恢复场景的三维结构。本文详细介绍了双目立体视觉的几何原理，并与 `camera_calibrator_double.py` 代码进行了对应分析，包括：

1. 相机模型与内参矩阵
2. 畸变校正原理
3. 双目标定与外参
4. 立体视觉的几何关系（对极约束、三角测量、视差与深度）
5. 立体校正方法
6. 三维点重建与误差评估

这些理论与实践的结合，为理解和应用双目立体视觉技术提供了全面的参考。